[user]
    name = William Volin
    email = me@williamvolin.com
[core]
    editor = nvim
    excludesfile = /Users/eein/.global_ignore
[push]
    default = simple
[github]
    user = eein
[alias]
    ; cleanupremote = git branch -r --merged | grep -v master | grep -v develop | sed 's/origin\///' | xargs -n 1 git push --delete origin
    ; cleanup = branch --merged | grep -v master | grep -v develop | xargs -n 1 git branch -d
